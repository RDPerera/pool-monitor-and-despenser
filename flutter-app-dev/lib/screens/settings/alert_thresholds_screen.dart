import 'package:flutter/material.dart';

class AlertThresholdsScreen extends StatelessWidget {
  const AlertThresholdsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Alert Thresholds')),
      body: const Center(child: Text('Alert thresholds placeholder')),
    );
  }
}
