import 'package:flutter/material.dart';

class DispensingScheduleScreen extends StatelessWidget {
  const DispensingScheduleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dispensing Schedule')),
      body: const Center(child: Text('Dispensing schedule placeholder')),
    );
  }
}
