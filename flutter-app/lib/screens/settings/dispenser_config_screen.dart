import 'package:flutter/material.dart';

class DispenserConfigScreen extends StatelessWidget {
  const DispenserConfigScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dispenser Configuration')),
      body: const Center(child: Text('Dispenser configuration placeholder')),
    );
  }
}
