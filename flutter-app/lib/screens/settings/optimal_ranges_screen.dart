import 'package:flutter/material.dart';

class OptimalRangesScreen extends StatelessWidget {
  const OptimalRangesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Optimal Ranges')),
      body: const Center(child: Text('Optimal ranges placeholder')),
    );
  }
}
